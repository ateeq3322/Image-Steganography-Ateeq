# Image Steganography Application

## Usage
1. Place `cover.png` and `secret.png` in same folder
2. Run `steganography_app.py`
3. Hidden image will be created as `hidden.png`
4. Run decode function to extract secret image
